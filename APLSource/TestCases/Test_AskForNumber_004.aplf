 R←Test_AskForNumber_004(stopFlag batchFlag);⎕TRAP
⍝ Exercise "AskForNumber" with `AskForNumber_Answers` but no fit
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :If batchFlag
     →0,R←T._NoBatchTest
 :EndIf

 C.AskForNumber_Answers←1 2⍴'Does-not-fit' 'y'

 →T.GoToTidyUp 0≠≢({1}C.AskForNumber)'Just press <enter>'

 R←T._OK

∆TidyUp:
 C.Cleanup
⍝Done
